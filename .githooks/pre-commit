#!/usr/bin/env bash
set -euo pipefail

npm run lint
npm run build

if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --redact
else
  echo "gitleaks is not installed; install it before committing sensitive work." >&2
  exit 1
fi
